"""Temporary patch module (not executed).

If you want a quick workaround without touching legacy imports inside
backend/services/session_manager.py, you can create a top-level `services`
package that re-exports backend.services.* modules.

This file exists only to document the approach.
"""

